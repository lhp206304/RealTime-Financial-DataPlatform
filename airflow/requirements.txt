# 本文件只放「在 airflow 容器里直接 import」的依赖（BashOperator 跑的同步脚本等）。
# apache-airflow 与 spark provider 不写在这里：provider 必须 --no-deps 装，
# 否则 pip 会拖入 pyspark（~400MB 完整 Spark 发行版），而本镜像用 /opt/spark 调 spark-submit，
# 根本用不到 pip 版 pyspark，还会撑爆构建磁盘。见 Dockerfile。
pydantic-settings>=2.0
redis>=5.0
clickhouse-connect>=0.7
structlog>=24.1,<26     # 结构化 JSON 日志（shared/log.py）
# ---- generator 每日造数（BashOperator 在本容器直接跑 /opt/generator）----
boto3>=1.34            # 连 MinIO 读写 parquet
pandas>=2.2,<3         # 不要升到 3.x：Airflow 2.9 按 pandas 2.x 适配，强升会破坏调度器
pyarrow>=17            # pandas 读写 parquet 的底层引擎
